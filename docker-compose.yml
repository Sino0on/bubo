services:
  web:
    build: .
    command: >
      sh -c "python manage.py migrate --noinput &&
             python manage.py collectstatic --noinput &&
             gunicorn bubostore.wsgi:application --bind 0.0.0.0:8032 --workers 2"
    ports:
      - "8032:8032"
    volumes:
      - .:/app
      - media_volume:/app/media
      - static_volume:/app/staticfiles
    env_file:
      - .env
    restart: unless-stopped

volumes:
  media_volume:
  static_volume: